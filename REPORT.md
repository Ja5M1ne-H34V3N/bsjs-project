# 分布式并行遗传算法（岛模型）求解 TSP：pcb442 实验结果报告（精简版）

## 1. 实验目标
实现分布式并行遗传算法（MPI 岛模型），引入迁移机制参数：
- 迁移间隔（migration interval）
- 迁移数量（migration count）
- 迁移拓扑（topology：island / chain）
- 迁移策略（strategy：best / worst）

并在 `pcb442/pcb442.tsp` 上验证：并行岛模型是否**显著提升解质量**（最终 best distance 更小）。

---

## 2. 实验对象与指标
- 数据集：`pcb442/pcb442.tsp`（442 城市）
- 对比对象：
  - 串行：`tsp.c`（task1）
  - 并行：`mpitsp.c`（task2，MPI 岛模型 + 迁移）
- 指标：最终 `best distance`（越小越好）

---

## 3. 并行运行参数（本次实验）
MPI 运行参数（来自 `RESULTS.md`）：
- `mpirun -np 4 ./task2 2000 2 chain best`

含义：
- 进程数（岛数）P = 4
- 迁移间隔 = 2000 代
- 迁移数量 = 2 个体/次
- 迁移拓扑 = chain（环形链/ring）
- 迁移策略 = best（迁移最好个体）

---

## 4. 实验设计（30 次重复 + 显著性检验）
- 串行程序运行 30 次，得到 30 个最终 best distance
- MPI 程序运行 30 次，得到 30 个最终 best distance
- 原始数据：`results_raw.txt`
- 统计与检验结果：`RESULTS.md`
- 检验方法：Welch 双样本 t 检验（双侧，适用于方差不完全相等情形）

---

## 5. 数值结果（统计量）
下表数值来自 `RESULTS.md`：

| 项目 | 串行 (serial) | MPI（岛模型） |
|---|---:|---:|
| 样本数 n | 30 | 30 |
| 平均值 mean | 51587.133 | 51351.800 |
| 样本方差 var | 20837.568 | 17816.924 |

平均提升（解质量改进，越小越好）：
- mean 差值（serial - mpi）= 51587.133 − 51351.800 = **235.333**
- 相对提升约：235.333 / 51587.133 ≈ **0.456%**

---

## 6. Welch 双样本 t 检验（双侧）展示

### 6.1 检验目的（要验证什么）
- 原假设 H0：两组（串行 vs MPI）的总体均值相同，即 μ_serial = μ_mpi
- 备择假设 H1：两组总体均值不同，即 μ_serial ≠ μ_mpi（双侧检验）

> 由于两组方差不要求相等，本实验采用 Welch t 检验。

### 6.2 Welch t 检验计算形式（展示）
记两组样本均值/方差/样本数为：
- 串行：$\bar{x}_1$、$s_1^2$、$n_1$
- MPI：$\bar{x}_2$、$s_2^2$、$n_2$

Welch t 统计量：
$$
t=\frac{\bar{x}_1-\bar{x}_2}{\sqrt{\frac{s_1^2}{n_1}+\frac{s_2^2}{n_2}}}
$$

Welch 自由度（近似）：
$$
df=\frac{\left(\frac{s_1^2}{n_1}+\frac{s_2^2}{n_2}\right)^2}{\frac{\left(\frac{s_1^2}{n_1}\right)^2}{n_1-1}+\frac{\left(\frac{s_2^2}{n_2}\right)^2}{n_2-1}}
$$

p-value（双侧）：
$$
p=2\cdot\Pr\left(T_{df}\ge |t|\right)
$$

### 6.3 本实验代入结果（来自 `RESULTS.md`）
- t = **6.556078**
- df = **57.648**
- p-value = **1.6646e-08**

判定（α=0.05）：
- p-value ≪ 0.05，拒绝 H0，说明**两组均值差异极显著**

---

## 7. 对结果的分析
1. **解质量对比（越小越好）**  
   MPI 平均 best distance = 51351.800，小于串行平均 51587.133，说明 MPI 岛模型得到的路径更短、解更优。

2. **改进幅度**  
   平均提升（serial - mpi）= **235.333**，相对提升约 **0.456%**。  
   对于 pcb442 这种规模的实例，该改进幅度在多次随机重复试验下仍能稳定出现。

3. **统计显著性**  
   Welch t 检验给出 p≈1.66e-08，表明“MPI 更优”并非偶然随机波动导致，而是**具有统计学显著性**的差异。

4. **为什么并行岛模型可能更优（直观解释）**  
   多岛并行会产生多条相对独立的搜索轨迹；通过迁移把某些岛的优秀个体引入其他岛，可以在保持多样性的同时加速优良结构（子路径）的传播，从而更容易跳出局部最优并获得更好的最终解。

---

## 8. 最终结论
- 若“优化”指 **解质量（最终 best distance 更小）**：**是，且提升显著**（p = 1.6646e-08）。
- 本报告验证的是“解质量改进”，不是“运行时间加速”；若需证明加速，需要额外采集每次运行 wall time 再做统计比较。
